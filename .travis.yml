language: python

python:
- "2.6"
- "2.7"
- "3.3"

install:
- pip install -q -r requirements.txt --use-mirrors
- pip install coveralls

script: coverage run --source=youtube manage.py test

after_success:
    coveralls
